# v25 — защита Firebase reads в журнале актов

Заменить файлы:

- `acts/akt-journal.html`
- `acts/akt-admin.html`

Что изменено:

- журнал больше НЕ читает акты при открытии страницы;
- смена менеджера/статуса/типа/даты/лимита больше НЕ делает запрос в Firebase;
- запрос к `acts` идёт только по кнопке **Загрузить**;
- после изменения фильтров показывается подсказка, но reads не идут;
- старый `akt-admin.html` тоже обновлён, чтобы старый путь не жёг лимиты.

Это не трогает клиентскую форму `/act-client.html` и не трогает старые задачи.
